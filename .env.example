# meow-bot runtime configuration (see spec §15.3).
# Copy this file to `.env` and fill in real values before booting the stack.

# Public hostname Caddy should serve and that GitHub webhooks reach.
MEOW_DOMAIN=

# GitHub App credentials (created via the manifest flow, spec §4.3).
GITHUB_APP_ID=
GITHUB_WEBHOOK_SECRET=
# Path inside the container/process to the GitHub App private key PEM.
GITHUB_APP_PRIVATE_KEY_PATH=/secrets/github-app.pem

# Mistral Workflows API key (workflow.run / worker triggers, spec §7).
MISTRAL_API_KEY=

# Daytona API key for ephemeral sandboxes (spec §8).
DAYTONA_API_KEY=

# Optional: bot login used by the receiver to drop self-deliveries.
# Leave empty until you know the GitHub App's bot login (e.g. `meow-bot[bot]`).
MEOW_BOT_LOGIN=
